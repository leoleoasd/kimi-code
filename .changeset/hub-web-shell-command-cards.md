---
'@moonshot-ai/kimi-code': minor
---

Hub web: render `!` shell commands as a terminal card — the command shows as a `$ cmd` row and the output as a stdout/stderr panel — instead of dumping the raw `<bash-input>`/`<bash-stdout>`/`<bash-stderr>` XML wrapper into a user bubble.
