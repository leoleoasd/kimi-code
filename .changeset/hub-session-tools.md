---
"@moonshot-ai/kimi-code": minor
---

Add the ListHubSessions and SendHubMessage tools for sessions bridged to a kimi hub: the agent can list every agent (and its sessions) on the hub and exchange plain-text messages between sessions. Connect via `kimi remote connect` or the TUI `/remote connect` to use them.
