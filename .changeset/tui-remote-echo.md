---
"@moonshot-ai/kimi-code": patch
---

When a session is also driven from the web UI (for example via `/remote connect`), the TUI now shows prompts sent from the web instead of skipping them, and transcript views no longer show a spurious "rolled back" ribbon after every prompt.
