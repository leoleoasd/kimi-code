---
"@moonshot-ai/kimi-code": patch
---

Name the session in hub turn-finished and waiting-for-input notifications: titles now read as "`<session title>` finished" instead of the engine agent id "main"; subagents keep their id as a suffix.
